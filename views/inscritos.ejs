<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Estudiantes Inscritos</title>
    <style>
        /* Estilos basados en el CSS proporcionado */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #007bff;
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #007bff;
            color: #fff;
        }
        .inscrito {
            font-weight: bold;
            color: #28a745; /* Color verde para resaltar estudiantes inscritos */
        }
        .btn {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 15px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 14px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 4px;
        }
        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Estudiantes Inscritos</h1>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Correo</th>
                    <th>Carrera</th>
                    <th>Inscrito</th>
                </tr>
            </thead>
            <tbody>
                <% estudiantesInscritos.forEach(est => { %>
                    <tr>
                        <td><%= est.id %></td>
                        <td><%= est.nombre %></td>
                        <td><%= est.correo %></td>
                        <td><%= est.carrera %></td>
                        <td class="inscrito"><%= est.inscrito ? "Sí" : "No" %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        <a href="/estudiantes" class="btn">Volver a la Página Principal</a>
    </div>
</body>
</html>
